services:
  - type: web
    name: pyrolysis-furnace-intelligence
    runtime: python
    plan: free
    buildCommand: pip install .
    startCommand: gunicorn app.app:server --bind 0.0.0.0:$PORT
    autoDeploy: true
